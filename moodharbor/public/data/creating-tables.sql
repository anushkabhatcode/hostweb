CREATE TABLE [dbo].[moods](
	[id] [int] NOT NULL,
	[Mood] [varchar](500) NULL,
	[type] [varchar](500) NULL
) ON [PRIMARY]
GO
ALTER TABLE [dbo].[moods] ADD PRIMARY KEY CLUSTERED 
(
	[id] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, SORT_IN_TEMPDB = OFF, IGNORE_DUP_KEY = OFF, ONLINE = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
GO


CREATE TABLE Movies (
    MovieID INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(255),
    MoodID INT,
    Image_Name NVARCHAR(255),
    FOREIGN KEY (MoodID) REFERENCES Moods(ID)
);

CREATE TABLE Books (
    BookID INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(255),
    MoodID INT,
    Image_Name NVARCHAR(255),
    FOREIGN KEY (MoodID) REFERENCES Moods(ID)
);

CREATE TABLE VacationSpots (
    SpotID INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(255),
    MoodID INT,
    Image_Name NVARCHAR(255),
    FOREIGN KEY (MoodID) REFERENCES Moods(ID)
);

--create table quotes--
CREATE TABLE [dbo].[Quotes](
	[id] [int] identity(1,1)  NOT NULL,
	[Quotes] [varchar](max) NULL,
	[Moodid] [int] NULL,
    FOREIGN KEY (MoodID) REFERENCES Moods(ID)
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO
ALTER TABLE [dbo].[Quotes] ADD PRIMARY KEY CLUSTERED 
(
	[id] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, SORT_IN_TEMPDB = OFF, IGNORE_DUP_KEY = OFF, ONLINE = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
GO
